// Escala de espaçamento simples usada nas telas novas (Home, Perfil, Login
// reestilizado). Segue os valores mais comuns observados no bundle de design
// (múltiplos de 4, com alguns saltos de 6/10/14 já usados no HTML original).
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
};

export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  pill: 999,
};

export default spacing;
