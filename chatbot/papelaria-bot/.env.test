# Valores FALSOS, só pra suíte de testes conseguir subir em CI (runner limpo
# do GitHub Actions não tem o `.env` real — ele é gitignored de propósito).
# `src/config/env.js` falha rápido no import se essas variáveis obrigatórias
# não existirem; nenhum teste hoje faz chamada de rede real (Supabase/
# Evolution API são sempre mockados), então nenhum destes valores precisa
# resolver de verdade (T4.1/T4.2, 27/08/2026).
#
# NUNCA colocar aqui uma credencial real — este arquivo é versionado.
EVOLUTION_API_URL=http://localhost:9999
EVOLUTION_API_KEY=fake-evolution-key-ci
EVOLUTION_INSTANCE=fake-instance-ci
WEBHOOK_SECRET_TOKEN=fake-webhook-token-ci
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_KEY=fake-supabase-service-key-ci
PHONE_CHEFE=5500000000000
PHONE_COMPRAS=5500000000000
PHONE_SERVICOS=5500000000000
PHONE_VANESSA=5500000000000
